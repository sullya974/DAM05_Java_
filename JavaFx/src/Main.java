import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {
        /*// Création d'un objet JFrame
        JFrame frame = new JFrame("FrameDemo");

        //Choix du process de fermeture du cadre
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        // Créer des instances de composants et les attacher au frame
        JLabel emptyLabel = new JLabel("Hello World !");
        frame.getContentPane().add(emptyLabel, BorderLayout.CENTER);

        //Dimensionner le cadre, définir ses props, titre
        frame.pack();

        //Afficher
        frame.setVisible(true);*/

        new JFrameCenter();
    }
}
